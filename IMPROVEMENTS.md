# Improvements

## Input & UX polish

- [ ] **Safari autofill polish on hidden input**
  - Change hidden capture input from `type="text"` to `type="tel"`
  - Remove `autocomplete="one-time-code"` unless OTP behavior is intentionally desired
  - Add `autocomplete="off"`, `autocorrect="off"`, `autocapitalize="off"`, and `spellcheck="false"`
  - Verify behavior on Safari macOS and iOS
